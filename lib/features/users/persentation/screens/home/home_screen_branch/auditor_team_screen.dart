import 'package:flutter/material.dart';

class AuditorTeamScreen extends StatelessWidget {
  const AuditorTeamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("AuditorTeamScreen"),
    );
  }
}
